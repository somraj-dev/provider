// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Define data models in Phase 1
// model User { ... }
// model Patient { ... }
// model Organization { ... }
// model Appointment { ... }
