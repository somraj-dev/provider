# Application Icons

Place application icons here:

- `icon.png` — 512x512 PNG for Linux
- `icon.ico` — Multi-size ICO for Windows
- `icon.icns` — ICNS for macOS

These will be referenced in `forge.config.ts`.
