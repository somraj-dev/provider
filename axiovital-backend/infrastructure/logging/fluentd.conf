# ============================================
# Fluentd Configuration (Placeholder)
# ============================================
# Centralized logging configuration

<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

# TODO: Configure log parsing and routing

<match axiovital.**>
  @type opensearch
  host opensearch
  port 9200
  logstash_format true
  logstash_prefix axiovital-logs
  # TODO: Configure authentication
</match>
