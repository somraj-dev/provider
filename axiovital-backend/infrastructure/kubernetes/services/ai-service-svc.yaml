# AI Service
apiVersion: v1
kind: Service
metadata:
  name: ai-service-svc
  namespace: axiovital
spec:
  selector:
    app: ai-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
