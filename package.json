{
  "name": "axiovital-provider",
  "version": "0.1.0",
  "private": true,
  "description": "AxioVital Provider Operating Environment — Enterprise-grade healthcare desktop platform",
  "license": "UNLICENSED",
  "workspaces": [
    "axiovital-frontend",
    "axiovital-backend",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=axiovital-frontend",
    "dev:gateway": "npm run start:dev --workspace=axiovital-backend",
    "build:frontend": "npm run build --workspace=axiovital-frontend",
    "build:gateway": "npm run build --workspace=axiovital-backend",
    "build:packages": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,css}\"",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "prisma": "^6.7.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0"
  }
}
