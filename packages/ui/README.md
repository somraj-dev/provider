# @axiovital/ui

Shared UI component library for the AxioVital Provider Operating Environment.

## Usage

```tsx
import { Button } from '@axiovital/ui';
```

## Development

Components are built with React and TypeScript. They are designed to be consumed by the frontend and desktop applications.
